/**
 * Initialize Variables
 */

// set global room variables here
// ...

// scale up the GUI to match the view
// which was scaled up to match the port
// var xScale = view_wport / view_wview[0];
// var yScale = view_hport / view_hview[0];
// display_set_gui_maximise(xScale, yScale, 0, 0);

